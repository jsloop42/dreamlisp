(def! test-in-user (fn* () 123))

(defmodule foo (export all))

(def! greet-name (fn* (n) "Hello Olivia"))

(def! greet (fn* () "Hello there"))

(defun fringe-me () 42)

(defun magic (n) (if (= n 42) "Now you see me" "Now you don't"))

(defmacro! fdefun (fn* (symname args & form) `(def! ~symname (fn* ~args (do ~@form)))))

(defmodule bar (export all) (import (from foo (magic 1))))

(defun do-magic () (* (magic 42) 0))

(defun magic-magic (n) (* (magic 42) n))

(defun bar (x) (str "I am " x))
